# Alpha Production Environment Variables
# HelpDesk Alpha Release - Local Production Setup

# Django Core Settings
DJANGO_SECRET_KEY=alpha-prod-helpdesk-2025-secure-key-change-for-real-production
DEBUG=False
ALPHA_MODE=True
ALLOWED_HOST=localhost

# Database Configuration
DB_NAME=helpdesk_alpha_db
DB_USER=postgres
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=5432

# Email Configuration (Alpha - Console Backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@helpdesk-alpha.local
EMAIL_SUBJECT_PREFIX=[HelpDesk Alpha] 

# Security Settings
SESSION_COOKIE_AGE=1800
CSRF_COOKIE_SAMESITE=Strict
SESSION_COOKIE_SAMESITE=Strict

# Logging
LOG_LEVEL=WARNING
SECURITY_LOG_LEVEL=WARNING

# Alpha Production Info
DEPLOYMENT_TYPE=alpha
VERSION=1.0.0-alpha
ENVIRONMENT=alpha-production